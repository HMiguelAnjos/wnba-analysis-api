# Porta do servidor (Railway injeta automaticamente — não precisa setar)
PORT=8000

# URLs do frontend autorizadas a chamar a API (separe por vírgula)
# Em produção: coloque a URL do seu frontend no Railway/Vercel/etc.
ALLOWED_ORIGINS=https://seusite.com,https://www.seusite.com
